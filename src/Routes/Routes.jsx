import React from 'react';
import {Routes as Routers, Route} from 'react-router-dom';

const Routes = ({user}) => {
    return (
        <>
            <Routers>
                <Route path='/' element={<></>}/>
            </Routers>
        </>
    )
}

export default Routes;
